<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container_message_receive2"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="0dp"
    android:layout_marginEnd="40dp"
    android:layout_marginTop="0dp">

    <!-- Background untuk bubble -->
    <TextView
        android:id="@+id/text_message_receive"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="0dp"
        android:layout_marginTop="14dp"
        android:layout_marginBottom="14dp"
        android:layout_marginEnd="16dp"
        android:gravity="start|top"
        android:includeFontPadding="true"
        android:padding="10dp"
        android:lineHeight="16.94dp"
        android:text="Your message here..."
        android:textColor="#FF000000"
        android:textSize="14sp"
        android:background="@drawable/image_message_receive_b_g2"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
